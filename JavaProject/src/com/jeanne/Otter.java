package com.jeanne;

abstract class Otter implements Mammal,Animal{

	
}
//Abstract methods can have same signature within interfaces but default methods are not allowed with same signature. If they are given then need to override in the inherited class